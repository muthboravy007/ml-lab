# ml-lab

A reproducible Python Project skeleton for the AI engineering program.

## Run it in there commans

```bash
git clone https:/github.com/muthboravy007/ml-lab.git
cd ml-lab && uv sync
uv run pytest

```
